#include <stdio.h>
#include <stdlib.h>
int zs(int n);
int main() {
	int n,k,i,a,b,c;
	int x[30];
	scanf("%d%d",&n,&k);
	for (i=1;i<=n;++i)
		scanf("%d",&a[i]);
	for(a=1;a<=)
	return 0;
}
int zs(int n){
	int j,a=0;
	for(j=2;j<=sqrt(n);++j)
		{if(n%j==0)
			++a;
		}
	if(a==0)
		return 1;
	else return 0;
}

